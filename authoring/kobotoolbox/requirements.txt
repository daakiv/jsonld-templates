# Dependencies for the KoboToolbox -> SSSOM -> JSON-LD metadata-authoring pipeline only.
#
# This file does not define dependencies for other projects in the
# ohdsi-gis-metadata-template repository.
#
# Minimum supported Python: 3.10
# Recommended Python: 3.11+
#
# From authoring/kobotoolbox/:
#   python -m pip install -r requirements.txt

# Core transformation runtime
jsonpath-ng>=1.5
PyYAML>=6.0
ply>=3.11

# Required only for pulling live KoboToolbox records
requests>=2.28

# Optional validation / RDF export
# Install only when needed:
#   python -m pip install sssom rdflib
#
# sssom   # enables vanilla_sssom.py --validate
# rdflib  # enables Turtle export from vanilla_sssom.py
